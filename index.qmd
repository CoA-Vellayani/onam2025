```{=html}
<div style="text-align: center; margin-bottom: 1em;">
  <img src="www/banner.webp"
       alt="Banner"
       style="width: 100%; max-height: 200px; object-fit: cover;">
</div>
```
```{=html}
<!-- Onam welcome marquee (HTML-only block for Quarto) -->
<div class="onam-marquee" role="region" aria-label="Onam welcome">
  <div class="onam-track">
    <span>🌼കോളേജ് ഓഫ് അഗ്രികൾച്ചർ വെള്ളായണി സംഘടിപ്പിക്കുന്ന സംയുക്ത ഓണാഘോഷ പരിപാടിയിലേക് സ്വാഗതം 🌼</span>
    <span aria-hidden="true">🌼കോളേജ് ഓഫ് അഗ്രികൾച്ചർ വെള്ളായണി സംഘടിപ്പിക്കുന്ന സംയുക്ത ഓണാഘോഷ പരിപാടിയിലേക് സ്വാഗതം  </span>
  </div>
</div>

<style>
  .onam-marquee{
    overflow:hidden;
    border-radius:12px;
    border:1px solid #e7e7e7;
    background:
      radial-gradient(600px 200px at 85% -20%, #fffbe6 0%, transparent 60%),
      linear-gradient(90deg,#fffdf4,#fff);
    box-shadow:0 8px 24px rgba(0,0,0,.06);
  }
  .onam-track{
    display:flex;
    gap:3rem;
    white-space:nowrap;
    will-change: transform;
    animation:onam-scroll 5s linear infinite;
    padding:.85rem 1rem;
    font-family: "Noto Sans Malayalam", system-ui, -apple-system, "Segoe UI", Roboto, Arial, "Noto Sans", sans-serif;
    font-weight:700;
    font-size:clamp(1rem, 2.4vw, 1.35rem);
    color:#1a5c2a;
  }
  .onam-track:hover{ animation-play-state: paused; } /* pause on hover */
  @keyframes onam-scroll{
    from{ transform: translateX(0); }
    to  { transform: translateX(-50%); } /* duplicate text for seamless loop */
  }
</style>
```   
പൂക്കളം മത്സരം രാവിലെ 8:00 മുതൽ 11:00 മണി വരെയാണ് . ഓരോ ടീമുകൾക്കും താഴെ രജിസ്റ്റർ ചെയ്യാവുന്നതാണ്👇
  
Pookalam competition is from 8:00 AM to 11:00 AM. The participating team should register by clicking below 👇  


```{=html}  
<!-- Responsive Pookalam card — mobile-friendly (Statoberry style) -->
<a class="pookalam-card" href="pookalamreg.html" aria-label="Open registration">
  <picture>
    <!-- serve webp if available, fallback to png/jpg -->
    <source srcset="www/pookalam.webp" type="image/webp">
    <img
      class="pookalam-img"
      src="www/pookalam.webp"
      alt="Onam pookalam"
      loading="lazy"
      decoding="async"
      sizes="(max-width: 480px) 96vw, (max-width: 1024px) 60vw, 480px">
  </picture>
  <span class="pookalam-badge" role="button" tabindex="0">REGISTRATION</span>
</a>

<style>
  :root{
    --sb-ink:#1b1b1b;
    --sb-brand:#6b46c1;         /* statoberry purple vibe */
    --sb-ink-on:#ffffff;
    --sb-card:#ffffff;
    --sb-shadow:0 10px 30px rgba(0,0,0,.12);
    --sb-radius:16px;
  }

  .pookalam-card{
    position:relative;
    display:block;
    width:min(96vw, 520px);
    border-radius:var(--sb-radius);
    overflow:hidden;
    text-decoration:none;
    background:var(--sb-card);
    box-shadow:var(--sb-shadow);
    isolation:isolate;
    /* maintain pleasant aspect on mobile even before image loads */
    aspect-ratio: 16 / 10;
  }

  .pookalam-img{
    position:absolute;
    inset:0;
    width:100%;
    height:100%;
    object-fit:cover;
    transform:scale(1.001); /* avoids subpixel gaps on some browsers */
    transition: transform .45s ease;
    will-change: transform;
  }

  /* gradient veil for legible badge on bright photos (always on mobile) */
  .pookalam-card::after{
    content:"";
    position:absolute; inset:0;
    background: linear-gradient(180deg, rgba(0,0,0,.10) 0%, rgba(0,0,0,.35) 75%, rgba(0,0,0,.55) 100%);
    pointer-events:none;
    opacity:.55;
    transition: opacity .35s ease;
  }

  /* registration badge (touch friendly) */
  .pookalam-badge{
    position:absolute;
    left:50%; bottom:14px;
    transform:translateX(-50%);
    display:inline-flex;
    align-items:center; justify-content:center;
    padding:.70rem 1.1rem;
    border-radius:999px;
    font:600 0.98rem/1 system-ui, -apple-system, "Segoe UI", Inter, Roboto, Arial, sans-serif;
    letter-spacing:.02em;
    color:var(--sb-ink-on);
    background:var(--sb-brand);
    box-shadow:0 6px 16px rgba(107,70,193,.35);
    border:1px solid rgba(255,255,255,.18);
    transition: transform .2s ease, box-shadow .2s ease, background .2s ease;
    user-select:none;
  }

  /* bigger tap target on small screens */
  @media (max-width: 520px){
    .pookalam-badge{ padding:.85rem 1.2rem; font-size:1.02rem; bottom:12px; }
  }

  /* hover/focus states (desktop) */
  .pookalam-card:hover .pookalam-img,
  .pookalam-card:focus-visible .pookalam-img{ transform:scale(1.05); }
  .pookalam-card:hover::after,
  .pookalam-card:focus-visible::after{ opacity:.35; }
  .pookalam-card:hover .pookalam-badge,
  .pookalam-card:focus-visible .pookalam-badge{
    box-shadow:0 10px 26px rgba(107,70,193,.45);
    transform:translateX(-50%) translateY(-1px);
  }

  /* optional: show center overlay text on larger screens when hovering */
  @media (hover:hover) and (pointer:fine){
    .pookalam-badge{
      bottom:16px;
    }
    .pookalam-card:hover .pookalam-badge{
      bottom:50%; transform:translate(-50%, 50%); /* center on hover */
      padding:1rem 1.4rem; font-size:1.05rem;
    }
  }

  /* accessibility: respect reduced motion */
  @media (prefers-reduced-motion: reduce){
    .pookalam-img,
    .pookalam-card::after,
    .pookalam-badge{ transition:none !important; }
  }
</style>
```
